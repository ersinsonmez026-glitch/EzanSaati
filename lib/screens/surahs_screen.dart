import 'package:flutter/material.dart';

class SurahsScreen extends StatelessWidget {
  const SurahsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sureler')),
    );
  }
}
